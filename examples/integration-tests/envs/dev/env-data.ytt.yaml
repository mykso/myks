#@data/values
---
environment:
  id: mykso-dev
  #! applications:  # already defined one level above
  #!  - proto: httpbingo # already defined one level above
  applications:
    - proto: ytt-render-test
      name: argocd-tests
    - proto: helm-render-test
      name: helm-installation
    #! A KCL-identifier prototype dir: `myks migrate` gives it a base schema. The explicit
    #! name keeps the application name (and its rendered paths) independent of the rename.
    - proto: per_chart_override
      name: per-chart-override
    - proto: multiple-sources
    - proto: multiple-contents-sources
    - proto: ytt-render-test
      name: ytt-installation
    - proto: static-test
    - proto: helm-myks-context
    - proto: lib-test
