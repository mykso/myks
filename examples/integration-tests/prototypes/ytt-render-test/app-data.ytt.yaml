#! Test the environment data library loading and usage
#@ load("@env-data.lib.yaml", "env_data")

#@data/values-schema
#@overlay/match-child-defaults missing_ok=True
---
application:
  baseValue: true
  envId: #@ env_data.environment.id
