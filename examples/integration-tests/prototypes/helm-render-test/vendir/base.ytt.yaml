#@ load("@ytt:data", "data")

#@ app = data.values.application
---
apiVersion: vendir.k14s.io/v1alpha1
kind: Config
directories:
  - path: #@ "charts/" + app.name
    contents:
      - path: .
        directory:
          path: ../_lib/charts/render-test-chart
#!
#! Test access to the wider data context
#! If a value is not available, ytt will fail with an error message
#!
#@ protoValue = data.values.application.baseValue
#@ envValue = data.values.environment.id
#@ globalValue = data.values.myks.gitRepoBranch
