#@data/values-schema
---
#@overlay/match-child-defaults missing_ok=True
application:
  destination:
    #! Destination is a Kubernetes cluster where the application will be deployed.
    #! The value depends on how the cluster is registered in ArgoCD.
    name: in-cluster
  #! Namespace monitored by ArgoCD.
  namespace: argocd
