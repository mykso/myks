"""karma-dashboards — L4a: namespace targets karma's resolved namespace,
set at the env scope where both apps are in one eval scope."""

import lib.schema as s

app = s.App {name = "karma-dashboards", proto = "dashboards"}
